# apps/mobile — Capacitor sobre apps/site

Não é uma segunda SPA. Contém só `capacitor.config.ts`, plataformas nativas e plugins (push, share). O `webDir` aponta para o build de `apps/site`. Entra em F6.
